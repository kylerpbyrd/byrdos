# Grafana datasource provisioning — Prometheus.
# This file is mounted into Grafana at /etc/grafana/provisioning/datasources.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
